#ifndef PROCESSMANAGER_H
#define PROCESSMANAGER_H
#include <vector>
#include <strings.h>

// #include <main.cpp>

bool createProgram(const string, vector<Instruction>);
int runProcessManager(int fd);

#endif